'use strict';

/**
 * @ngdoc function
 * @name vizApp.controller:MenuCtrl
 * @description
 * # MenuCtrl
 * Controller of the vizApp
 */
angular.module('vizApp')
  .controller('MenuCtrl', function ($scope) {
    $scope.items = [{
    }];
  });
